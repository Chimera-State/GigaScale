syntax = "proto3";

package reservation.v1;

option go_package = "github.com/Chimera-State/GigaScale/internal/pb/reservationv1;reservationv1";

service ReservationService {
  rpc ReserveSeat(ReserveSeatRequest) returns (ReserveSeatResponse) {}
}

message ReserveSeatRequest {
  string user_id = 1;
  string trip_id = 2; // setnx için
  int32 seat_number = 3;
}

message ReserveSeatResponse {
  bool success = 1; //koltuk dolu mu boş mu
  string message = 2; //başarılı veya koltuk dolu
}
